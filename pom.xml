<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.3</version>
    <relativePath/>
  </parent>

  <groupId>team.acg.access</groupId>
  <artifactId>asset-platform-build</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>

  <name>Asset Platform Build</name>
  <description>Builds the frontend and the executable Java artifact for the GitLab Maven release pipeline.</description>

  <modules>
    <module>backend</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>17</java.version>
    <frontend-maven-plugin.version>1.15.1</frontend-maven-plugin.version>
    <node.version>v16.20.2</node.version>
    <npm.version>8.19.4</npm.version>
    <ecp-sdk-java.version>1.0.3-release</ecp-sdk-java.version>
    <vite.ecp.app-code>WLY5YG</vite.ecp.app-code>
    <vite.ecp.api-base-url>/api/v1</vite.ecp.api-base-url>
    <vite.ecp.config-source-mode>local</vite.ecp.config-source-mode>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.idanchuang.ecp</groupId>
        <artifactId>ecp-sdk-spring-boot-starter</artifactId>
        <version>${ecp-sdk-java.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <inherited>false</inherited>
        <configuration>
          <filesets>
            <fileset>
              <directory>${project.basedir}/frontend-dist</directory>
            </fileset>
          </filesets>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <inherited>false</inherited>
        <executions>
          <execution>
            <id>clean-devops-artifact</id>
            <phase>pre-clean</phase>
            <configuration>
              <target>
                <delete file="${project.basedir}/dist" quiet="true"/>
                <delete dir="${project.basedir}/dist" quiet="true"/>
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>${frontend-maven-plugin.version}</version>
        <inherited>false</inherited>
        <configuration>
          <workingDirectory>${project.basedir}</workingDirectory>
          <installDirectory>${project.build.directory}/frontend</installDirectory>
          <environmentVariables>
            <VITE_ECP_APP_CODE>${vite.ecp.app-code}</VITE_ECP_APP_CODE>
            <VITE_ECP_API_BASE_URL>${vite.ecp.api-base-url}</VITE_ECP_API_BASE_URL>
            <VITE_ECP_AUTH_CONFIG_SOURCE_MODE>${vite.ecp.config-source-mode}</VITE_ECP_AUTH_CONFIG_SOURCE_MODE>
          </environmentVariables>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
              <npmVersion>${npm.version}</npmVersion>
            </configuration>
          </execution>
          <execution>
            <id>npm-ci</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>ci</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-validate-authz</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run validate:authz</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-build</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
